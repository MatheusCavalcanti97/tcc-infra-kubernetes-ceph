apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: banco
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block-ec
  resources:
    requests:
      storage: 10Gi