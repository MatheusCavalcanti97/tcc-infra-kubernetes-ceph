apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-pvc-teste
  namespace: rook-ceph

spec:
  accessModes:
    - ReadWriteMany

  storageClassName: ceph-filesystem

  resources:
    requests:
      storage: 1Gi